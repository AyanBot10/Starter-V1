module.exports = {
  config: {
    //name: "edited_message"
    name: "unavailable_event"
  },
  start: async function({ api, message, event }) {
    /**
     * This command won't do anything
     * 
     * This is just to show how it looks
     */
  }
}